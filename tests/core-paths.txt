# Globs (repo-root-relative) that make the Stop hook relevant. A change touching
# any of these is "feature work" and must arrive with a validated test case.
#
# DELIBERATELY EMPTY until Week 1 lands application code. With no globs the hook
# fails open and never blocks - that is correct, not a gap.
#
# Note: these are fnmatch globs, and fnmatch's `*` crosses directory separators.
# `app/**` matches at any depth under app/ and does NOT match `application/...`.
# Avoid bare patterns like `*.py`, which would match at every depth in the repo.
#
# Uncomment as each module appears:
app/api/**
app/core/**
app/db/**
app/modules/**
migrations/**
# Operational entry point, not part of the request path -- but it writes rows to the
# users table, so a change to it is feature work by any honest definition. Without
# this glob it could be altered with nothing gating it.
app/cli.py
