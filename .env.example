# Copy to .env and fill in real values. .env is gitignored; this file is not.
# Populate as services are added (Postgres, Redis, RabbitMQ, Kafka, Temporal,
# and the Part B LLM provider / vector DB).
